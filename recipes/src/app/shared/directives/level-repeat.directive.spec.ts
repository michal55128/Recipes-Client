import { LevelRepeatDirective } from './level-repeat.directive';

describe('LevelRepeatDirective', () => {
  it('should create an instance', () => {
    const directive = new LevelRepeatDirective();
    expect(directive).toBeTruthy();
  });
});
