import { TimePreparationPipe } from './time-preparation.pipe';

describe('TimePreparationPipe', () => {
  it('create an instance', () => {
    const pipe = new TimePreparationPipe();
    expect(pipe).toBeTruthy();
  });
});
